---
import ErrorServer from '@sections/ErrorServer.astro';
import Layout from '@/Layout.astro';

export const prerender = false;
---

<Layout
    description="Server error. The atlas could not load this page. Retry the request in a few moments, or return to the home page to search and filter markers and journeys."
    noindex
    title={'Server Error \u2014 Travel'}
>
    <ErrorServer />
</Layout>
