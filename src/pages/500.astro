---
import ErrorServer from '@sections/ErrorServer.astro';
import Layout from '@/Layout.astro';

export const prerender = false;
---

<Layout
    description="Server error. The Travel atlas could not load this page. Retry the request in a few moments, or return to the atlas to browse every marker and journey."
    noindex
    title={'Server Error \u2014 Travel'}
>
    <ErrorServer />
</Layout>
