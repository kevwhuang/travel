---
import ErrorServer from '@sections/ErrorServer.astro';
import Layout from '@/Layout.astro';

export const prerender = false;
---

<Layout
    description={'The Travel atlas hit a server error while loading this page. Nothing is wrong on your end \u2014 retry in a moment or return to the map of plotted places.'}
    noindex
    title={'Server Error \u2014 Travel'}
>
    <ErrorServer />
</Layout>
